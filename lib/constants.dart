import 'package:flutter/material.dart';

const backgroundColor = Color(0xff100B20);
// const Duration transitionDuration = Duration(seconds: 2);
const EdgeInsets padding = EdgeInsets.symmetric(horizontal: 24, vertical: 16);
